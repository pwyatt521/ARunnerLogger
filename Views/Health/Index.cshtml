@model IEnumerable<HealthModel>
@{
    ViewData["Title"] = "HealthLogs";
}
<body>
    <h1>
        Health Logs
    </h1>
    <h2>
        <div>
            <a asp-area="" asp-controller="Health" asp-action="PostBlog">Post Blog</a>
        </div>
    </h2>
<ul class="list-group">
    @foreach (var adventure in Model)
    {
        <li class="list-group-item">
            <div class="row">
                <div class="col-md-8">
                    <p>
                        <a href="@Url.Action("View", "Health", new { @id = adventure.ID })">@adventure.AName</a>
                    </p>
                </div>
                <div class="col-md-2">
                    <p>
                        <a href="@Url.Action("View", "Health", new { @id = adventure.ID })">@adventure.Rating</a>
                    </p>
                </div>
                <div class="col-md-2">
                    <form class ="form-inline" asp-action="Delete" asp-controller="Health" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="id" value="@adventure.ID" />
                        <button type="submit" class="btn btn-danger"><i class="fas fa-times"></i></button>
                    </form>
                </div>
            </div>
        </li>

    }
</ul>
</body>